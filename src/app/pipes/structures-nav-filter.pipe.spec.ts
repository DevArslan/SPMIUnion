import { StructuresNavFilterPipe } from './structures-nav-filter.pipe';

describe('StructuresNavFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StructuresNavFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
