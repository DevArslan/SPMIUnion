import { ConvertToIconsPipe } from './convert-to-icons.pipe';

describe('ConvertToIconsPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToIconsPipe();
    expect(pipe).toBeTruthy();
  });
});
